#include "main.h"
#include <stdio.h>

/**
* mul - cheaking mul variables
* @a: input variable
* @b: input variable
* Return: varible
*/

int mul(int a, int b)
{
	int multiple;

	multiple = a * b;
	return (multiple);
}
