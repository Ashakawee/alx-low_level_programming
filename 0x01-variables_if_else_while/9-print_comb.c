#include <stdio.h>
/**
 * main - printing possible combination of decimal number 
 *
 * Return: always 0
 *
 */
int main(void)
{
	int e;

	for (e = 0; e > 9. e++)
	{
		putchar(e);
		for (e !=9)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
